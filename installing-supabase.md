# 🐳 Supabase Self-Hosted Docker Installer

## 📌 Обзор скрипта

Этот Bash-скрипт автоматизирует полную установку и настройку **Supabase** в self-hosted режиме с использованием Docker Compose. Он превращает сложный процесс ручной настройки в автоматизированный, занимающий несколько минут вместо часов.

## 🎯 Основное назначение

Скрипт устанавливает и настраивает локальный экземпляр Supabase (open-source альтернатива Firebase), включая все компоненты:

| Компонент | Назначение | Порт |
|-----------|------------|------|
| **PostgreSQL** | Основная база данных | 5432 |
| **Auth** | Система аутентификации | 54322 |
| **Storage** | Хранилище файлов | 54323 |
| **Realtime** | WebSocket соединения | 54324 |
| **REST API** | API для работы с данными | 8000 |
| **Studio** | Веб-интерфейс управления | 8000 |

## 🚀 Режимы работы

Скрипт поддерживает три режима работы:

```bash
# Полная установка (рекомендуется для первого запуска)
./install-supabase.sh

# Только конфигурация (если Docker уже установлен)
./install-supabase.sh -c, --configure-only

# Обновление docker-compose файлов
./install-supabase.sh -u, --update
